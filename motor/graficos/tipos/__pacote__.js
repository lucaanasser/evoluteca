/* Tipo: { plano, serie(pincel, serie, estilo), preparar?, extremos? }. */

import { linhas } from "./linhas.js";
import { histograma } from "./histograma.js";

export const tipos = { linhas, histograma };
